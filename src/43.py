def add_numbers(a: int, b: int) -> int:
    """
    Add two numbers.

    Parameters:
    a (int): First number.
    b (int): Second number.

    Returns:
    int: The sum of a and b.
    """
    return a + b

# Example usage
result = add_numbers(2, 3)
print("Result:", result)
